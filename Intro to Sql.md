Course Goals
Understand databses and their strucutre
Extract information from databases using sql

Structured Query Language (SQL)

Tables:
Table naming :
CLear 
refer to the data
lowercase
use underscore
Data types:

Sql Keywords:
SELECT "name" FROM

SELECT name 
FROM patrons;
"*" selects all 

aliasing "as"
Example:
SELECT name AS n
FROM employee
SQL Flavours:
they have an iso but they have different extra

TYpes: postgresql
free 
open source
name of the databse ansq sql flavor
sql server free and paid created with microsft
T-sql flavour
Example
in postfre sql limit
